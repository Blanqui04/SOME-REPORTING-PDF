apiVersion: v1
kind: Service
metadata:
  name: {{ include "grafana-reporter.fullname" . }}
  labels:
    {{- include "grafana-reporter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "grafana-reporter.selectorLabels" . | nindent 4 }}
